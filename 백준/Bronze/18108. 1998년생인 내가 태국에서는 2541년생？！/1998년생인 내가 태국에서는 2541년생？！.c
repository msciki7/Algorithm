#include <stdio.h>
int main(void) {
    int y;                    // 1. 정수 변수 y 선언
    scanf("%d", &y);          // 2. 정수를 입력받아서 변수 y에 저장
    printf("%d", y - 543);    // 3. y-543의 결과값 출력

    return 0;
}